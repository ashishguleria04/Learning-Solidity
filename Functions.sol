//Function is basically a repeatable task

function get() public view returns (uint) {

    return count;
}

//this above is a sort of a syntax where "get()" is function name
// and "public" and "view" are function type
// and "returns (uint)" is return types
// and between "{}" these brackets, one defines their logic, I think I got the point lol